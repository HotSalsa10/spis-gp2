SPIS -- Full Project Summary (Phases 1-6)
==========================================
Smart Pharmacy Inventory System
Graduation Project 2
Team: Saleh, Nawaf, Mazen, Ali
Date: March 2026


========================================================================
PHASE 1 -- Project Setup & Data Ingestion
========================================================================

WHAT WAS DONE
  Set up the entire project structure, installed all dependencies,
  downloaded real pharmacy sales data from Kaggle, and loaded it into
  a local SQLite database.

THE DATA
  - Source: Kaggle pharmacy sales dataset (from a Turkish pharmacy)
  - 424,080 sales records spanning Jan 2014 to Oct 2019
  - 8 drug categories (ATC codes):
      M01AB  -- Anti-inflammatory (e.g., Diclofenac)
      M01AE  -- Anti-inflammatory (e.g., Ibuprofen)
      N02BA  -- Pain relief (e.g., Aspirin)
      N02BE  -- Pain relief (e.g., Paracetamol)
      N05B   -- Anxiolytics (e.g., Diazepam)
      N05C   -- Sleep aids (e.g., Zolpidem)
      R03    -- Respiratory (e.g., Salbutamol)
      R06    -- Antihistamines (e.g., Cetirizine)
  - 57 individual drugs total, 25 flagged as critical
  - Data comes in 4 granularities: hourly, daily, weekly, monthly

DATABASE (data/inventory.db)
  4 tables:
    atc_categories  -- 8 rows (one per drug category)
    drugs           -- 57 rows (individual drug products)
    sales           -- 424,080 rows (all sales records)
    atc_inventory   -- 8 rows (current stock per ATC code, added Phase 4)

  This file is git-ignored. To recreate it:
    python scripts/ingest_kaggle.py

PROJECT STRUCTURE
  spis/               -- Main Python package
    data/             -- Data loading, database, pipeline
    models/           -- ML models (forecaster, risk classifier)
    api/              -- Flask REST API (Phase 5)
    dashboard/        -- Streamlit app (Phase 6)
  tests/              -- Automated test suite
  scripts/            -- CLI entry points (ingest, pipeline, train, api)
  data/raw/           -- Raw CSV files (git-ignored)
  data/processed/     -- Pipeline output CSVs (git-ignored)
  models/             -- Trained model files (git-ignored)
  docs/               -- Documentation and summaries
  notebooks/          -- Jupyter notebooks for exploration

KEY FILES
  spis/data/catalog.py     -- Drug catalog with ATC codes and names
  spis/data/database.py    -- SQLite schema definition and seeding
  spis/data/generator.py   -- Data generation utilities
  scripts/ingest_kaggle.py -- Downloads Kaggle data and loads into DB


========================================================================
PHASE 2 -- Data Pipeline & Feature Engineering
========================================================================

WHAT WAS DONE
  Built an automated pipeline that takes raw daily sales from the
  database, fills date gaps, engineers 35 time-series features, and
  splits the data into train/test sets for the ML model.

PIPELINE STEPS
  1. Extract daily sales (16,848 rows = 8 ATC codes x 2,106 days)
  2. Validate (check nulls, negatives, duplicates)
  3. Fill missing dates with quantity=0
  4. Engineer 35 features (see below)
  5. Split into train (before 2019) and test (2019 onwards)

OUTPUT FILES (data/processed/ -- git-ignored)
  train.csv          -- 14,544 rows (Jan 2014 - Dec 2018)
  test.csv           -- 2,248 rows (Jan 2019 - Oct 2019)
  features_daily.csv -- 16,848 rows (full dataset with features)

  To recreate:
    python scripts/run_pipeline.py

THE 35 ENGINEERED FEATURES
  All derived from the sales data itself -- nothing external except
  the Turkey holiday and school calendars.

  Calendar (12):
    day_of_week        -- Monday=0 through Sunday=6
    day_of_month       -- 1st through 31st
    month              -- January=1 through December=12
    year               -- 2014 through 2019
    week_of_year       -- Week 1 through 52
    is_weekend         -- 1 if Saturday/Sunday, else 0
    is_holiday         -- 1 if Turkish public holiday, else 0
    season             -- 1=Winter, 2=Spring, 3=Summer, 4=Fall
    is_payday_window   -- 1 if day 1-3 or 15-17 of the month
    is_school_holiday  -- 1 if during Turkish school break
    quarter            -- 1 through 4
    days_to_month_end  -- Days remaining until end of month

  Lags (7) -- "What were the sales X days ago?"
    lag_1              -- Yesterday's sales
    lag_2              -- 2 days ago
    lag_3              -- 3 days ago
    lag_7              -- Same day last week
    lag_14             -- Two weeks ago
    lag_28             -- Four weeks ago
    lag_365            -- Same day last year

  Rolling windows (12) -- Averages/stats over recent days:
    rolling_mean_7     -- Average sales over last 7 days
    rolling_std_7      -- How much sales varied in last 7 days
    rolling_mean_14    -- Average over last 14 days
    rolling_mean_28    -- Average over last 28 days
    rolling_std_28     -- Variance over last 28 days
    rolling_min_7      -- Lowest day in last week
    rolling_max_7      -- Highest day in last week
    rolling_mean_90    -- Average over last 3 months
    rolling_mean_365   -- Average over last year
    ema_7              -- Exponential moving average (7-day)
    ema_14             -- Exponential moving average (14-day)
    ema_28             -- Exponential moving average (28-day)

  Derived (4):
    lag_ratio_7        -- Yesterday's sales / 7-day average
                          (detects demand spikes)
    trend_counter      -- Days since dataset start
                          (captures long-term growth/decline)
    rolling_range_7    -- rolling_max_7 - rolling_min_7
                          (measures recent demand volatility)
    ema_ratio          -- ema_7 / ema_28
                          (captures short vs long-term trend direction)

KEY FILES
  spis/data/pipeline.py    -- All pipeline logic
  scripts/run_pipeline.py  -- CLI entry point
  tests/test_pipeline.py   -- 7 tests (all pass)


========================================================================
PHASE 3 -- XGBoost Forecasting Model
========================================================================

WHAT WAS DONE
  Trained an XGBoost machine learning model that predicts daily drug
  sales. Compared it against two simple baselines to prove it actually
  adds value.

HOW IT WORKS
  Input:  36 features (the 35 above + drug category encoded as number)
  Output: Predicted sales quantity for that drug on that day

  The model learns patterns like:
  - "Sales of painkillers go up on weekdays"
  - "If last week's average was high, this week will probably be high too"
  - "Sales dip on Turkish holidays and school breaks"
  - "Demand spikes around payday (1st and 15th of each month)"

TRAINING PROCESS
  1. Load train/test CSVs from Phase 2
  2. Encode drug categories as integers (M01AB=0, M01AE=1, etc.)
  3. Drop first-year rows where lag_365 has no data
  4. Try 512 hyperparameter combinations using GridSearchCV with
     TimeSeriesSplit (5 folds) to find the best settings
  5. Evaluate on the held-out 2019 test data

RESULTS
  Model               MAE       RMSE      MAPE(%)
  --------------------------------------------------
  Naive (lag_1)       4.23      7.23      98.42
  Moving Avg (7d)     2.89      4.98      67.83
  XGBoost             1.07      2.49      18.80

  MAE  = Average prediction error (lower is better)
  RMSE = Like MAE but penalises big misses more
  MAPE = Average error as a percentage

  XGBoost achieves MAE 1.07, beating the 7-day moving average by 63%.

TOP FEATURES (what the model relies on most)
  ema_14            0.41   (14-day exponential moving average)
  ema_7             0.36   (7-day exponential moving average)
  ema_28            0.04   (28-day exponential moving average)

BEST HYPERPARAMETERS
  n_estimators:      500
  max_depth:         6
  learning_rate:     0.1
  subsample:         0.8
  colsample_bytree:  0.8
  min_child_weight:  5
  reg_alpha:         0

SAVED ARTIFACTS (models/ -- git-ignored)
  xgboost_forecaster.joblib  -- The trained model
  label_encoder.joblib       -- Drug category name-to-number mapping
  metrics.json               -- Results table in JSON

  To recreate:
    python scripts/train_model.py

KEY FILES
  spis/models/forecaster.py  -- All ML logic
  scripts/train_model.py     -- CLI entry point
  tests/test_forecaster.py   -- 8 tests (all pass)


========================================================================
PHASE 4 -- Risk Classification & Order Recommendations
========================================================================

WHAT WAS DONE
  Added a risk classification layer on top of the forecaster.
  For each drug category the system answers:
    1. What is the current risk tier?
    2. How many units should we order to cover the next 30 days?

RISK TIERS (Days of Stock = current_stock / daily_demand)
  CRITICAL  : DoS < 3 days  -- likely stockout within 3 days
  LOW       : 3 to 7 days
  OK        : 7 to 30 days
  OVERSTOCK : 30+ days

ORDER QUANTITY FORMULA
  safety_buffer = daily_demand * safety_days  (default safety_days = 3)
  order_qty     = max(0, forecast_30d + safety_buffer - current_stock)

RESULT OBJECT (RiskAssessment -- immutable dataclass)
  atc_code      : Drug category code
  current_stock : Units on hand
  forecast_30d  : Predicted demand over next 30 days
  daily_demand  : forecast_30d / 30
  days_of_stock : current_stock / daily_demand (null if demand = 0)
  risk_tier     : CRITICAL | LOW | OK | OVERSTOCK
  order_qty     : Recommended units to order

KEY FILES
  spis/models/risk_classifier.py  -- All risk logic
  scripts/assess_risk.py          -- CLI entry point
  tests/test_risk_classifier.py   -- 19 tests (all pass)

  To run:
    python scripts/assess_risk.py


========================================================================
PHASE 5 -- Flask REST API
========================================================================

WHAT WAS DONE
  Exposed the forecasting and risk classification logic as HTTP
  endpoints. External systems (or the Streamlit dashboard in Phase 6)
  can now query SPIS over a standard REST API.

ENDPOINTS
  GET /health
    Returns {"status": "ok", "version": "0.1.0"}
    Always HTTP 200 if the server is running.

  GET /api/v1/risk
    Runs a full risk assessment for all 8 ATC codes.
    Returns:
      assessed_at  : ISO-8601 UTC timestamp
      safety_days  : Safety buffer used in order-qty calculation
      results      : List of per-ATC-code risk records (see Phase 4)
    HTTP 503 if model artifacts are not loaded.

  GET /api/v1/forecast/<atc_code>
    Returns a 30-day demand forecast for a single ATC code.
    Example: GET /api/v1/forecast/M01AB
    Returns:
      atc_code       : The code requested
      forecast_30d   : Total predicted demand over 30 days
      daily_demand   : forecast_30d / 30
      forecast_start : First date of the forecast window (ISO date)
    HTTP 404 if the ATC code is unknown.
    HTTP 503 if model artifacts are not loaded.

APP DESIGN
  - Flask app factory pattern: create_app(config=None)
  - Model artifacts loaded once at startup (eager loading)
  - If artifacts are missing, server starts but returns 503 on ML routes
  - Routes registered via Blueprint for clean separation

TO START THE SERVER
  python scripts/run_api.py
  python scripts/run_api.py --host 0.0.0.0 --port 8080 --debug

  Requires model artifacts. Run these first if needed:
    python scripts/ingest_kaggle.py
    python scripts/run_pipeline.py
    python scripts/train_model.py

KEY FILES
  spis/api/app.py      -- Flask app factory (create_app)
  spis/api/routes.py   -- All route handlers (Blueprint)
  scripts/run_api.py   -- CLI server launcher
  tests/test_api.py    -- 18 tests (all pass)


========================================================================
FULL TEST SUITE
========================================================================

75 tests total, all passing:

  tests/test_pipeline.py (7 tests):
    - Validates clean data handling
    - Validates duplicate detection
    - Tests missing date filling
    - Tests feature column creation
    - Tests lag value correctness
    - Tests train/test split has no data leakage
    - Tests train/test split has no date overlap

  tests/test_forecaster.py (8 tests):
    - Tests ATC encoding creates integer column
    - Tests encoding is consistent between train/test
    - Tests naive baseline uses lag_1
    - Tests moving average baseline uses rolling_mean_7
    - Tests perfect predictions give zero error
    - Tests known error values are calculated correctly
    - Tests XGBoost training returns a valid model
    - Tests save/load roundtrip produces same predictions

  tests/test_risk_classifier.py (19 tests):
    - Tests all 4 risk tier boundaries
    - Tests order quantity formula
    - Tests zero-demand edge case (infinite DoS)
    - Tests RiskAssessment dataclass immutability
    - Tests forecast_30_days determinism
    - Tests unknown ATC code raises ValueError
    - Tests inventory loading from SQLite

  tests/test_api.py (18 tests):
    - Tests /health returns 200 with status and version
    - Tests /api/v1/risk returns 200 with all required fields
    - Tests all 8 ATC codes appear in risk results
    - Tests risk tier values are valid
    - Tests order_qty is non-negative
    - Tests metadata (assessed_at, safety_days) present
    - Tests /api/v1/risk returns 503 when model absent
    - Tests /api/v1/forecast/<code> returns 200 and all fields
    - Tests forecast values are non-negative
    - Tests unknown ATC code returns 404 with error field
    - Tests /api/v1/forecast returns 503 when model absent

  tests/test_database.py (6 tests):
    - Tests all 4 tables are created by init_db
    - Tests init_db is idempotent (safe to call twice)
    - Tests atc_categories seeded with correct row count
    - Tests drugs seeded with correct total and critical counts
    - Tests atc_inventory seeded with correct row count
    - Tests sales table starts empty

  tests/test_ingest_data.py (11 tests):
    - Tests _load_and_normalise returns 5 expected columns
    - Tests row count matches CSV input
    - Tests granularity argument propagated to all rows
    - Tests negative quantities clipped to 0
    - Tests custom column-name mapping works
    - Tests missing column triggers SystemExit
    - Tests _get_db_atc_codes returns empty set for empty DB
    - Tests _get_db_atc_codes returns correct codes
    - Tests _register_unknown_codes inserts into atc_categories
    - Tests _register_unknown_codes inserts into atc_inventory (stock=0)
    - Tests _register_unknown_codes is idempotent

  tests/test_register_atc.py (6 tests):
    - Tests _infer_levels on standard 5-char ATC code
    - Tests _infer_levels on level-5 7-char code
    - Tests _infer_levels on single-letter anatomical code
    - Tests _infer_levels normalises lowercase input
    - Tests _infer_levels on 2-char code shorter than level-2
    - Tests _infer_levels strips leading/trailing whitespace

  Run all tests:
    python -m pytest tests/ -v


========================================================================
DEPENDENCIES (requirements.txt)
========================================================================

  numpy>=1.26         -- Numerical computing
  pandas>=2.1         -- Data manipulation
  scikit-learn>=1.4   -- ML utilities (GridSearchCV, LabelEncoder)
  xgboost>=2.0        -- The forecasting model
  holidays>=0.40      -- Turkish public holiday calendar
  spacy>=3.7          -- NLP (for drug name processing)
  scispacy>=0.5.4     -- Medical NLP
  flask>=3.0          -- REST API (Phase 5)
  streamlit>=1.32     -- Dashboard (Phase 6)
  kagglehub>=0.3      -- Kaggle dataset download
  pytest>=7.4         -- Testing framework


========================================================================
HOW TO SET UP FROM SCRATCH
========================================================================

  1. Clone the repo
  2. Create virtual environment:
       py -3.11 -m venv venv
  3. Activate:
       .\venv\Scripts\activate        (PowerShell)
       source venv/Scripts/activate    (bash)
  4. Install dependencies:
       pip install -r requirements.txt
  5. Download data and build database:
       python scripts/ingest_kaggle.py
  6. Run the data pipeline:
       python scripts/run_pipeline.py
  7. Train the model:
       python scripts/train_model.py
  8. Run tests:
       python -m pytest tests/ -v
  9. Start the API server:
       python scripts/run_api.py


========================================================================
PHARMACY-AGNOSTIC SCALABILITY (added after Phase 5)
========================================================================

WHAT WAS DONE
  Decoupled the system from the Kaggle dataset so any pharmacy can
  load their own historical sales data and use the full SPIS pipeline.

CHANGES
  spis/data/pipeline.py
    Removed hardcoded check for the 8 Kaggle ATC codes from validate().
    The pipeline now accepts any set of ATC codes from any pharmacy.
    EXPECTED_ATC_CODES constant kept for test fixtures only.

  scripts/ingest_data.py  (NEW)
    Generic CSV ingester. Expects long/tidy format:
        date,atc_code,quantity
        2023-01-01,M01AB,45.0
    Column names configurable via --date-col / --atc-col / --qty-col.
    --register flag auto-registers unknown ATC codes so new pharmacies
    can onboard without a manual pre-registration step.

  scripts/register_atc.py  (NEW)
    Manually register a new ATC drug category before ingesting data.
    Inserts into atc_categories + atc_inventory with full metadata.
    Safe to re-run (INSERT OR IGNORE). --list prints all codes.

HOW TO ONBOARD A NEW PHARMACY
  1. Register each new drug category:
       python scripts/register_atc.py --code A10BA --name "Biguanides" --stock 200
     Or auto-register everything in bulk:
       python scripts/ingest_data.py --csv sales.csv --register

  2. Load historical sales data:
       python scripts/ingest_data.py --csv sales.csv

  3. Rebuild the feature pipeline:
       python scripts/run_pipeline.py

  4. Retrain the XGBoost model:
       python scripts/train_model.py

  5. Start the API:
       python scripts/run_api.py


========================================================================
PHASE 7 -- Testing
========================================================================

WHAT WAS DONE
  Expanded the test suite to cover the database module and the two new
  pharmacy-agnostic ingestion scripts added after Phase 5.

NEW TEST FILES
  tests/test_database.py   (6 tests)
    Verifies init_db creates all 4 tables, seeds the correct number of
    rows, and is safe to call twice (idempotency).

  tests/test_ingest_data.py  (11 tests)
    Unit tests for the three helper functions in scripts/ingest_data.py:
      _load_and_normalise  -- columns, row count, granularity, negative
                              clipping, custom column names, missing column
      _get_db_atc_codes    -- empty DB and populated DB
      _register_unknown_codes -- inserts categories + inventory, idempotent

  tests/test_register_atc.py  (6 tests)
    Tests for _infer_levels() in scripts/register_atc.py covering
    standard, level-5, single-letter, lowercase, short, and whitespace
    edge cases.

KEY FILES
  tests/test_database.py
  tests/test_ingest_data.py
  tests/test_register_atc.py

  Total test suite: 75 tests, all passing.
    python -m pytest tests/ -v


========================================================================
PHASE 6 -- Streamlit Dashboard
========================================================================

WHAT WAS DONE
  Built a single-page Streamlit dashboard that shows inventory risk
  tiers, 30-day demand forecasts, and recommended order quantities for
  all 8 ATC codes. Imports model artifacts directly (no running API
  required).

FILES
  spis/dashboard/app.py   -- Main Streamlit app (~100 lines)
  scripts/run_dashboard.py -- Convenience launcher

HOW TO RUN
  # Activate the virtual environment first, then:
  python scripts/run_dashboard.py
  # Opens at http://localhost:8501

  # Optional: custom port
  python scripts/run_dashboard.py --port 8502

  # Or directly via Streamlit:
  streamlit run spis/dashboard/app.py

WHAT IT SHOWS
  1. Summary row: four metric cards (CRITICAL / LOW / OK / OVERSTOCK counts)
  2. Risk table: all 8 ATC codes with stock, 30d forecast, daily demand,
     days-of-stock, risk tier (colour-coded rows), and order quantity
  3. Bar chart: recommended order quantities per ATC code

DESIGN DECISIONS
  - Imports directly from spis.models (not the Flask API) for simplicity.
  - Uses @st.cache_resource for model/encoder (loaded once per process).
  - Uses @st.cache_data(ttl=300) for assessment results (refreshed every 5 min).
  - Guard at startup: shows clear error if pipeline/model artifacts are missing.


========================================================================
WHAT'S NEXT
========================================================================

  Phase 8 -- GP Report
    Write the graduation project report following university guidelines.
